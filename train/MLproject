name: mlflow_pipeline

entry_points:
    train:
        parameters:
            upstream_dir_path: {type: string, default: ../../data/train}
        command: |
            python -m src.train \
                train.upstream_dir_path={upstream_dir_path}